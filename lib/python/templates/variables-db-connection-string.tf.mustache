# Copyright (c) 2024, 2025 Oracle Corporation and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl
# shellcheck disable=SC1091
# This is an auto-generated file.


variable "update_any_ds" {
  type = bool
  default = false
}

{{#datasources}}
#/////////////////
# Datasource #{{{datasourceName}}}
#/////////////////
variable "ds_{{{datasourceName}}}" {
  type = string
  default = ""
}

variable "update_ds_{{{datasourceName}}}" {}

# ATP DB Configuration
# Variable used in UI only
variable "atp_db_uses_private_endpoint_{{{datasourceName}}}" {
  type        = bool
  description = "Indicates if the ATP database uses private endpoint for network access"
  default     = false
}

variable "atp_db_network_compartment_id_{{{datasourceName}}}" {
  type        = string
  description = "The OCID of the compartment in which the ATP database private endpoint VCN is found"
  default     = ""
}

variable "atp_db_existing_vcn_id_{{{datasourceName}}}" {
    type        = string
    description = "The OCID of the VCN used by the ATP database private endpoint"
    default     = ""
}

variable "atp_db_compartment_id_{{{datasourceName}}}" {
  type        = string
  description = "The OCID of the compartment where the ATP database is located"
  default     = ""
}
variable "atp_db_id_{{{datasourceName}}}" {
  type        = string
  description = "The OCID of the ATP database"
  default     = ""
}

variable "atp_db_level_{{{datasourceName}}}" {
  type        = string
  description = "The ATP database level. Allowed values are low, tp, tpurgent"
  default     = "low"
  validation {
    condition     = contains(["low", "tp", "tpurgent"], var.atp_db_level_{{{datasourceName}}})
    error_message = "WLSC-ERROR: Invalid value for atp_db_level. Allowed values are low, tp and tpurgent."
  }
}

variable "db_strategy_{{{datasourceName}}}" {
  type=string
  default     = "Autonomous Transaction Processing Database"
  validation {
    condition     = contains(["Autonomous Transaction Processing Database", "Database System", "Edit JDBC String"], var.db_strategy_{{{datasourceName}}})
    error_message = "Accepted values are Autonomous Transaction Processing Database, Database System, Edit JDBC String"
  }
}

variable "oci_db_connection_string_{{{datasourceName}}}" {
  type=string
  description = "Oracle database connection string to connect to database. Example: //<scan_hostname>.<host_domain_name>:<db_port>/<pdb_or_sid>.<Host Domain Name>"
  default = ""
}

##### OCI Database Service #########

variable "oci_db_port_{{{datasourceName}}}" {
  type        = number
  description = "The listener port for the OCI database"
  default     = 1521
}

variable "oci_db_compartment_id_{{{datasourceName}}}" {
  type        = string
  description = "The OCID of the compartment of the OCI database"
  default     = ""
}

variable "oci_db_network_compartment_id_{{{datasourceName}}}" {
  type        = string
  description = "The OCID of the compartment in which the DB System VCN is found"
  default     = ""
}

variable "oci_db_existing_vcn_id_{{{datasourceName}}}" {
  type        = string
  description = "The OCID of the VCN used by the OCI database"
  default     = ""
}

variable "oci_db_dbsystem_id_{{{datasourceName}}}" {
  type        = string
  description = "The OCID of the OCI database system"
  default     = ""
}

# Variable used in UI only
variable "oci_db_dbhome_id_{{{datasourceName}}}" {
  type        = string
  description = "The OCID of the OCI database home"
  default     = ""
}

# Variable used in UI only  # What is this used for?
variable "oci_db_dbhome_major_version_{{{datasourceName}}}" {
  type        = string
  description = "Version of Database home in the DB System"
  default     = ""
}

variable "oci_db_database_id_{{{datasourceName}}}" {
  type        = string
  description = "The OCID of the OCI database"
  default     = ""
}

variable "oci_db_pdb_service_name_{{{datasourceName}}}" {
  type        = string
  description = "The name of the pluggable database (PDB) in which to provision the schemas for a JRF-enabled WebLogic Server domain. This is required for Oracle Database 12c or later"
  default     = ""
}

variable "existing_vcn_add_seclist_{{{datasourceName}}}" {
  description = "Whether to create a security list to open DB port for OCI DB."
  type        = bool
  default     = true
}
#/////////////////
# End Section: Datasource #{{{datasourceName}}}
#/////////////////
{{/datasources}}
